#!/usr/bin/env bash

if [ -f bin/pre_compile ]; then
  echo "-----> Running pre-compile hook"
  source ./bin/pre_compile
fi

sudo apt-get install -y libssl-dev=1.0.2g-1ubuntu4.8 openssl=1.0.2g-1ubuntu4.8